use rand::Rng; 

fn main() {
    
    let num = rand::thread_rng().gen_range(0..100);
    println!("{}", num);
    
    if num<50{
       println!("50'den küçük"); 
    } else {
       println!("50'den büyük"); 
    }
    
}
